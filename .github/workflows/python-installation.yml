name: CI Pipeline
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello GitHub Actions"

jobs:
  build:
    runs-on: ubuntu-latest

  test:
    runs-on: ubuntu-latest
     







